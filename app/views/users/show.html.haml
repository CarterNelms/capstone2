- if params[:id].to_i == current_user.id
  =link_to "My Library", user_libraries_path(current_user.id)
  =link_to "My Wanted Items", user_wanted_lists_path(current_user.id)
  =link_to "Edit Profile", edit_user_registration_path